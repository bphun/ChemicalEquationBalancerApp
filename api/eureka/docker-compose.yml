version: '2'

services:
  eureka:
    build: .
    ports:
      - "8761:8761"
    image: "bphun/chemical-equation-balancer:eureka"
    container_name: eureka
    networks:
      - chemBalancerNet

networks:
  chemBalancerNet: