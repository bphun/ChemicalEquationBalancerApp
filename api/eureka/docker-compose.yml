version: '2'

services:
  eureka:
    build: .
    ports:
      - "8761:8761"
    image: "bphun/chemical-equation-balancer:eureka"
    container_name: eureka
    networks:
      - chemBalancerNet
    mem_limit: 200m
    memswap_limit: 200m
    mem_reservation: 160m

networks:
  chemBalancerNet: