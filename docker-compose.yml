version: '2'

services:
  api-server:
    extends:
      file: api/docker-compose.yml
      service: api-server
      #docker-image-updater:
      #extends:
      #file: dockerImageUpdaterServer/docker-compose.yml
      #service: docker-image-updater
  grafana:
    extends:
      file: grafana/docker-compose.yml
      service: grafana
  prometheus:
    extends:
      file: prometheus/docker-compose.yml
      service: prometheus
  management-portal:
    extends:
      file: image-labeling-portal/docker-compose.yml
      service: management-portal
  nginx:
    extends:
      file: nginx/docker-compose.yml
      service: nginx
    depends_on:
        - api-server
          # - docker-image-updater
        - grafana
        - prometheus
        - management-portal

networks:
  chemBalancerNet:
